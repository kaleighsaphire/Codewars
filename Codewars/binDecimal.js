// Complete the function which converts a binary number (given as a string) 
// to a decimal number.

function binToDec(bin){
    let num = parseInt(bin, 2)
    return num;
  }

  // parseInt converts string to number. Passing in 2 arguments. 